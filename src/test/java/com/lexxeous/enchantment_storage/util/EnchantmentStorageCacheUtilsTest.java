package com.lexxeous.enchantment_storage.util;

import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

class EnchantmentStorageCacheUtilsTest {
	// region Confidence
	@Disabled("Requires Minecraft item bootstrap; cover this behavior in GameTests instead of plain JVM unit tests.")
	@Test
	void storeCostCache_bootstrapPlaceholder() {
		assertTrue(true);
	}
	// endregion

	// region Regression
	@Disabled("Requires Minecraft item bootstrap; cover this behavior in GameTests instead of plain JVM unit tests.")
	@Test
	void storeCostCache_regressionPlaceholder() {
		assertTrue(true);
	}
	// endregion
}
